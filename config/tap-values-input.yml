#@data/values

#@overlay/match-child-defaults missing_ok=True
---
tap:
  version: "1.2.0"
  namespace: tap-install
  catalogs: []

  registry:
    host: tapbuild.azurecr.io
    repositories:
      buildService: tap/build-service
      ootbSupplyChain: tap/supply-chain

  domains:
    main: tap.mc.tanzu.projectscions.com
    tapGui: tap-gui.tap.mc.tanzu.projectscions.com
